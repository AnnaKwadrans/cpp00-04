#ifndef ANIMAL_H
# define ANIMAL_H

# include <string>
# include <iostream>

class Animal
{
        private:
                std::string     _type;

// virtual sirve para indicar que una función puede ser sobrescrita (override) en clases derivadas.
// Solo tiene sentido aplicarlo a funciones miembro, no a variables ni como bloque de acceso.
        public:


};



#endif